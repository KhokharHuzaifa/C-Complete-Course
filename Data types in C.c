#include<stdio.h>
#include<conio.h>
int main(){
    // variables are used to store value in memory and call them by their name
    // variable name can be any name based on some rules
    int var1=20;
    int var2=10;
    printf("Sum of two varibales is %d",var1+var2);
}