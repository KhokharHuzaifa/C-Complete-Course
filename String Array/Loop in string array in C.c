#include<stdio.h>
int main(){
    // You can also loop through the characters of a string, using a for loop:
    // Examp
    char carName[] = "Volvo";
    for (int i = 0; i < 5; ++i) {
    printf("%c\n", carName[i]);
}
}
